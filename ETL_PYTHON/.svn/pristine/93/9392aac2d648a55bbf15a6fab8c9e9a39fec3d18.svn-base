import gc
import logging
import json
import os

from xinxiang import config
from xinxiang.util import my_duck, my_oracle, my_date, cons_error_code, cons, Params
from zeep import Client
from xinxiang.util import DetailEntity


def create_temp_table(duck_db):
    pass


def insert_target_table(duck_db_memory, pr_unit_id, ph_recipe, max_efct_time, expire_time, uuid, current_time, oracle_conn, ETL_Proc_Name):
    sql = """
          INSERT INTO APS_ETL_APC_RESULT (
                PARENTID,
                TOOL_ID,
                PPID,
                LIFE_TIME,
                LAST_RUN_TIME,
                UPDATE_TIME,
                PARTCODE) VALUES(
                '{uuid}', 
                '{pr_unit_id}', 
                '{ph_recipe}', 
                '{max_efct_time}',
                '{expire_time}',
                '{current_time}',              
                '')                                                                 
    """.format(pr_unit_id=pr_unit_id, ph_recipe=ph_recipe, max_efct_time=max_efct_time, expire_time=expire_time, uuid=uuid, current_time=current_time)

    my_duck.exec_sql(oracle_conn=oracle_conn,
                     duck_db_memory=duck_db_memory,
                     ETL_Proc_Name=ETL_Proc_Name,
                     methodName="Insert Into APS_ETL_APC_RESULT",
                     sql=sql,
                     current_time=current_time,
                     update_table="APS_ETL_APC_RESULT")

# 如果对象是一个字典，则遍历字典的键和值，并对值进行递归调用；如果对象是一个列表，则遍历列表中的每个元素，并对每个元素进行递归调用；对于其他类型的值，将其添加到列表中。
def traverse_json(obj, result_list):
    if isinstance(obj, dict):
        for key, value in obj.items():
            traverse_json(value, result_list)
    elif isinstance(obj, list):
        for item in obj:
            traverse_json(item, result_list)
    else:
        result_list.append(obj)


def execute():
    ###############################################################
    ### 以下参数必须定义
    ### ETL_Proc_Name    : ETL 名称
    ### current_time     ：请直接拷贝
    ### current_time_short ：请直接拷贝
    ### uuid             ：请直接拷贝
    ### target_table     : 该ETL输出表名
    ### used_table_list  : 该ETL使用到的，参考到的表名(中间表不算)
    ### target_table_sql ： 该ETL输出表定义SQL
    ###############################################################
    ETL_Proc_Name = "APS_ETL_BR.APS_ETL_APC_WEBSERVICE_20M"
    current_time = my_date.date_time_second_str()
    current_time_short = my_date.date_time_second_short_str()
    uuid = my_oracle.UUID()

    target_table = "APS_ETL_APC_RESULT"
    used_table_list = []
    target_table_sql = """
        create table {}APS_ETL_APC_RESULT
        (
            parentid        VARCHAR(32) not null,
            tool_id         VARCHAR(64),
            ppid            VARCHAR(1024),
            prod_id         VARCHAR(256),
            ope_no          VARCHAR(256),
            life_time       VARCHAR(32),
            last_run_time   VARCHAR(32),
            remaining_qtime VARCHAR(32),
            update_time     VARCHAR(32),
            partcode        VARCHAR(64)
        )
    """.format("")  # 注意:这里一定要这么写 [create table 表名] => [create table {}表名]
    target_db_file = my_duck.get_target_file_name(target_table, current_time_short)
    # -------------------------- 内存模式改成文件模式
    _temp_db_path = os.path.join(config.g_mem_speed_etl_output_path, target_table, "inprocess")
    if not os.path.exists(_temp_db_path):
        os.makedirs(_temp_db_path)

    temp_db_file = os.path.join(_temp_db_path, target_table + "_" + current_time_short + "_temp.db")
    # 处理中文件
    in_process_db_file = os.path.join(_temp_db_path, target_table + "_" + current_time_short + ".db")
    # 结果文件
    target_db_file = os.path.join(config.g_mem_etl_output_path, target_table,
                                  target_table + "_" + current_time_short + ".db")
    # --------------------------

    oracle_conn = None
    try:
        oracle_conn = my_oracle.oracle_get_connection()
        # 开始日志
        my_oracle.StartCleanUpAndLog(oracle_conn, ETL_Proc_Name, current_time)
        # -------------------------- 内存模式改成文件模式
        # 创建DuckDB
        duck_db_memory = my_duck.create_duckdb_in_file(_temp_db_path, in_process_db_file, target_table_sql)
        duck_db_memory.sql('SET threads TO 4')
        if not os.path.exists(os.path.join(config.g_mem_speed_etl_output_path, 'duck_temp', uuid)):
            os.makedirs(os.path.join(config.g_mem_speed_etl_output_path, 'duck_temp', uuid))
        duck_db_memory.execute(
            "SET temp_directory='{}'".format(os.path.join(config.g_mem_speed_etl_output_path, 'duck_temp', uuid)))

        if not config.g_debug_mode:
            create_temp_table(duck_db_memory)
        else:
            duck_db_temp = my_duck.create_duckdb_for_temp_table(_temp_db_path, temp_db_file)
            # 创建Temp表
            create_temp_table(duck_db_temp)
            duck_db_temp.commit()
            duck_db_temp.close()
            my_duck.attach_temp_db_write_able(duck_db_memory, "TEMPDB", temp_db_file)
        # --------------------------

        # 获取Webservice URL与Method等参数信息
        url = my_oracle.GetApsParamInfos(oracle_conn, 'ETL_CALL_WEBSERVICE', '').DIC_VALUE
        # 创建WSDL客户端
        client = Client(url)

        # 请求WebService服务并获取返回值
        result = client.service.ApcFurExpiredTimeInfoInquireWS()
        rs = json.loads(result)
        for json_ in rs:
            result_list = []
            traverse_json(json_, result_list)
            detailEntity = DetailEntity.DetailEntity()
            detailEntity.phRecipe = result_list[0]
            detailEntity.prUnitId = result_list[1]
            detailEntity.maxEfctTime = result_list[2]
            if result_list[3] is not None:
                detailEntity.expireTime = result_list[3]
            else:
                detailEntity.expireTime = ''
            print(detailEntity)
            ## 以下为业务逻辑
            ################################################################################################################
            insert_target_table(duck_db_memory,  detailEntity.prUnitId, detailEntity.phRecipe, detailEntity.maxEfctTime,
                                detailEntity.expireTime, uuid, current_time,
                                oracle_conn, ETL_Proc_Name)

            ## 以上为业务逻辑
            ################################################################################################################
        # 导出到目标文件中
        target_db_file = my_duck.export_result_duck_file_and_close_duck_db_memory2(duck_db_memory,
                                                                                   in_process_db_file=in_process_db_file,
                                                                                   target_table=target_table,
                                                                                   current_time=current_time_short)
        # 写版本号
        my_oracle.HandlingVerControl(oracle_conn, uuid, target_table, target_db_file, current_time_short)
        # 写完成日志
        my_oracle.EndCleanUpAndLog(oracle_conn, ETL_Proc_Name, current_time)
    except Exception as e:
        # 写警告日志
        my_oracle.SaveAlarmLogData(oracle_conn, ETL_Proc_Name, e, target_db_file, 'XETL030')
        logging.exception("{ETL_Proc_Name} 處理出錯 : {e}".format(ETL_Proc_Name=ETL_Proc_Name, e=e))
        # 导出到目标文件中
        target_db_file = my_duck.export_result_duck_file_and_close_duck_db_memory2(duck_db_memory,
                                                                                   in_process_db_file=in_process_db_file,
                                                                                   target_table=target_table,
                                                                                   current_time=current_time_short)
        raise e
    finally:
        oracle_conn.commit()
        oracle_conn.close()
        # 删除TMP目录:LQN:2023/08/21
        if os.path.exists(os.path.join(config.g_mem_etl_output_path, 'duck_temp', uuid)):
            os.remove(os.path.join(config.g_mem_etl_output_path, 'duck_temp', uuid))
        if os.path.exists(temp_db_file) and not config.g_debug_mode:
            os.remove(temp_db_file)
        gc.collect()  # 内存释放


if __name__ == '__main__':
    # 单JOB测试用
    print("start")
    execute()
